// bootgen input for converting gepiorig_bd_wrapper.bit (Vivado output) into
// gepiorig_bd_wrapper.bit.bin (FPGA-manager-loadable format used by xmutil/
// dfx-mgr on Zynq UltraScale+).
//
// Invoked indirectly via package_gepiorig.sh.

all:
{
  [destination_device = pl] gepiorig_bd_wrapper.bit
}
