// bootgen input for converting counter_bd_wrapper.bit (Vivado output) into
// counter_bd_wrapper.bit.bin (FPGA-manager-loadable format used by xmutil/
// dfx-mgr on Zynq UltraScale+).
//
// Invoked indirectly via package_counter.sh.

all:
{
  [destination_device = pl] counter_bd_wrapper.bit
}
