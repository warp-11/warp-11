// bootgen input for converting gepunit_bd_wrapper.bit (Vivado output) into
// gepunit_bd_wrapper.bit.bin (FPGA-manager-loadable format used by xmutil/
// dfx-mgr on Zynq UltraScale+).
//
// Invoked indirectly via package_gepunit.sh.

all:
{
  [destination_device = pl] gepunit_bd_wrapper.bit
}
