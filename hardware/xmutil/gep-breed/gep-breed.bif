// bootgen input for converting gepbreed_bd_wrapper.bit (Vivado output) into
// gepbreed_bd_wrapper.bit.bin (FPGA-manager-loadable format used by xmutil/
// dfx-mgr on Zynq UltraScale+).
//
// Invoked indirectly via package_gepbreed.sh.

all:
{
  [destination_device = pl] gepbreed_bd_wrapper.bit
}
