<Project Sdk="Microsoft.NET.Sdk">

    <PropertyGroup>
        <TargetFramework>net10.0-browser</TargetFramework>
        <RuntimeIdentifier>browser-wasm</RuntimeIdentifier>
        <OutputType>Exe</OutputType>
        <WasmMainJSPath>wwwroot/main.js</WasmMainJSPath>
        <WarningsAsErrors>FS0025</WarningsAsErrors>
        <!-- Same reasoning as Warp11.Tutorial.Browser: the 2.4 MB symbol map
             is fetched eagerly at boot by every visitor and cannot be withheld
             at deploy time, so it is not emitted. -->
        <WasmEmitSymbolMap>false</WasmEmitSymbolMap>
    </PropertyGroup>

    <ItemGroup>
        <Compile Include="Program.fs"/>
    </ItemGroup>

    <!-- `WasmMainJSPath` carries main.js into the bundle; the page that loads
         it has to be asked for by name or it is simply left behind. -->
    <ItemGroup>
        <WasmExtraFilesToDeploy Include="wwwroot/index.html"/>
    </ItemGroup>

    <ItemGroup>
        <ProjectReference Include="../Warp11.GolView/Warp11.GolView.fsproj"/>
    </ItemGroup>

    <ItemGroup>
        <PackageReference Include="Avalonia" Version="12.1.1"/>
        <PackageReference Include="Avalonia.Browser" Version="12.1.1"/>
        <PackageReference Include="Avalonia.Themes.Fluent" Version="12.1.1"/>
        <PackageReference Include="Avalonia.FuncUI" Version="2.0.0"/>
    </ItemGroup>

</Project>
